# Expo HAS CHANGED

Project pins **Expo SDK 54** (see `package.json`) because the test phone's Expo Go app maxes out at SDK 54. Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code. Do not bump the SDK without confirming the phone's Expo Go supports the new version.
